# ФИЛОСОФИЯ РАБОТЫ НАД ПРОЕКТОМ

> Этот документ описывает подход к работе над проектом Joy Pick Server. Все изменения должны соответствовать этим принципам.

## 🔒 Критически важные правила

### 1. Обработка ошибок
- **ВСЕГДА** передавать объект ошибки в функцию `error()` - 4-й параметр должен быть `err` или `error`
- **НИКОГДА** не возвращать просто "500 ошибка" без деталей
- Все ошибки должны возвращаться в JSON ответе API с детальной информацией:
  - `message` - текст ошибки
  - `error` - сообщение об ошибке
  - `errorName` - тип ошибки
  - `errorCode` - код ошибки (если есть)
  - `sqlMessage` - если SQL ошибка
  - `sql` - SQL запрос (если SQL ошибка)
  - `stack` - stack trace (в development режиме)
  - `errorDetails` - все детали ошибки
- **НИКОГДА** не предлагать проверить логи сервера (у пользователя нет доступа)

### 2. ID пользователей
- **ВСЕГДА** использовать UUID из базы данных (поле `id` из таблицы `users`)
- **НИКОГДА** не использовать Firebase UID для операций с БД
- Валидировать формат UUID перед использованием
- Проверять существование пользователя в БД перед использованием его ID

### 3. Даты и время
- Все даты хранятся и возвращаются в UTC
- Формат: ISO 8601 (`YYYY-MM-DDTHH:mm:ss.sssZ`)
- При возврате данных из БД нормализовать все даты через `normalizeDatesInObject()`

## 📋 Процесс работы

### Перед началом работы
1. **Прочитать CURRENT_STAGE.md** - понять текущее состояние проекта
2. **Прочитать IMPORTANT_NOTES.md** - знать критически важные правила
3. **Прочитать REQUEST_CONCEPT.md** - понимать логику заявок (если работаешь с заявками)
4. **Сделать резервные копии** - если планируются значительные изменения

### Перед внесением изменений
1. **Проверить связи и imports** - убедиться, что не создаются выдуманные переменные
2. **Задать вопросы самому себе:**
   - Как это изменение повлияет на другие части системы?
   - Есть ли зависимости от этого кода?
   - Нужно ли обновить документацию?
   - Нужно ли обновить тесты?
   - Соответствует ли это правилам из `.cursorrules`?
3. **Сделать 3 симуляции** процессов, которые находятся вокруг изменяемого модуля

### При работе с кодом
1. **Следовать существующим паттернам** - не изобретать велосипед
2. **Использовать существующие утилиты** - `error()`, `normalizeDatesInObject()`, `generateId()` и т.д.
3. **Не создавать дубликаты** - не создавать файлы с приставками `_fix`, `_final`, `_corrected`
4. **Обновлять документацию** - если изменяется API или логика, обновить `API_DOCUMENTATION.md`

### После внесения изменений
1. **Проверить линтер** - убедиться, что нет ошибок
2. **Проверить логику** - убедиться, что изменения работают как ожидается
3. **Обновить документацию** - если нужно
4. **Обновить CURRENT_STAGE.md** - отразить текущее состояние

## 🎯 Принципы разработки

### Простота
- Код должен быть понятным и простым
- Избегать излишней сложности
- Использовать существующие решения, если они подходят

### Консистентность
- Следовать существующим соглашениям
- Использовать одинаковые паттерны во всем проекте
- Поддерживать единый стиль кода

### Надежность
- Всегда обрабатывать ошибки
- Валидировать входные данные
- Проверять существование ресурсов перед использованием

### Документирование
- Обновлять документацию при изменении API
- Комментировать сложную логику
- Поддерживать актуальность CURRENT_STAGE.md

## 🚫 Что НЕ делать

1. **НЕ предлагать проверить логи сервера** - у пользователя нет доступа
2. **НЕ использовать Firebase UID** для операций с БД - только UUID из БД
3. **НЕ возвращать общие ошибки** - всегда возвращать детальную информацию
4. **НЕ создавать дубликаты файлов** - редактировать существующие
5. **НЕ игнорировать правила** из `.cursorrules` и `IMPORTANT_NOTES.md`
6. **НЕ изменять логику** без понимания контекста из `REQUEST_CONCEPT.md`

## 📚 Референсы

- [CURRENT_STAGE.md](./CURRENT_STAGE.md) - текущее состояние проекта
- [IMPORTANT_NOTES.md](./IMPORTANT_NOTES.md) - критически важные правила
- [.cursorrules](./.cursorrules) - правила для AI ассистента
- [REQUEST_CONCEPT.md](./REQUEST_CONCEPT.md) - концепция заявок
- [API_DOCUMENTATION.md](./API_DOCUMENTATION.md) - документация API

