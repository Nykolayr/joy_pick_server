# Где лежит мастер-флоу приложения

Основной документ по **UI, навигации и пользовательским шагам** ведётся в репозитории **Flutter-приложения**, а не здесь:

**`D:\Projects\joy_pick\joy_pick\docs\joy_pick_app_master_flows.md`**

(При соседнем клоне: `../joy_pick/docs/joy_pick_app_master_flows.md` от корня `joy_pick_server`.)

Этот серверный репозиторий — **дополнение**: `API_DOCUMENTATION.md`, `scripts/cronTasks.js`, миграции, RAG-чанки в `docs/knowledge/support_*`. Править чанки Support AI по сценариям «как в приложении» нужно **по клиентскому файлу выше**, сверяясь с API при необходимости.

Актуальная полная детализация: разделы **A–V** + **W** (охват, инвентарь доков, смоук-тесты, статусы, splash, карта/создание, **матрицы кнопок** waste/speed/event, `ParticipantCompletionPage`, профиль, админка, deeplink, две поддержки, партнёры/коины, чеклист чанков, индекс всех `*_page.dart`). **W** — выжимка **каждого корневого `.md` со скрина** joy_pick; **W.1** — полное оглавление всех разделов `API_DOCUMENTATION.md` (~8100 строк). Разделы **E/I/J/K** согласованы с `REQUEST_CONCEPT.md` с оговоркой про `cost` и крон.
