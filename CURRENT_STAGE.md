# ТЕКУЩЕЕ СОСТОЯНИЕ ПРОЕКТА

> **Важно:** Перед началом работы прочитай этот файл и все файлы, на которые он ссылается, по порядку.

## 📍 Где мы сейчас

### Последние изменения
- ✅ Добавлена логика продления заявок waste: через 1 день отправляется пуш (для тестирования, потом вернуть на 7 дней), через 2 дня удаление (если не продлена)
- ✅ Добавлены поля `expires_at` и `extended_count` в таблицу requests для управления сроком действия заявок
- ✅ Создан endpoint `POST /api/requests/:id/extend` для продления заявки waste еще на 1 день (для тестирования, потом вернуть на неделю, максимум одно продление)
- ✅ Обновлена cron задача: разделена на две части - `notifyInactiveWasteRequests` (пуш через 1 день) и `deleteInactiveRequests` (удаление через 2 дня)
- ✅ Исправлена обработка ID пользователей: везде используются только UUID из БД (поле `id` из таблицы `users`), не Firebase UID
- ✅ Добавлена валидация для `joined_user_id` и `actual_participants` - проверка формата UUID
- ✅ Обновлена документация API с указанием, что все ID должны быть UUID из БД
- ✅ Исправлено дублирование уведомлений в cron задачах: добавлена проверка существующих записей в `cron_actions`
- ✅ Исправлена логика SpeedCleanup: добавлен перевод денег исполнителю и пуш о получении донатов
- ✅ Создана концепция чатов: `CHAT_CONCEPT.md` - три типа чатов (support, private, group) с полной спецификацией

### Текущие задачи
1. **Критично:** Выполнить миграцию `database/add_expires_at_and_extended_count.sql` для добавления полей `expires_at` и `extended_count`
2. **Критично:** Убедиться, что на сервере обновлен файл `scripts/cronTasks.js` с правильным SQL синтаксисом
3. Проверить, что все места, где используются ID пользователей, работают с UUID из БД
4. Убедиться, что фронтенд отправляет UUID из БД, а не Firebase UID

## 📚 Референсы на важные файлы

### Критически важные документы (читать в первую очередь)
1. **[IMPORTANT_NOTES.md](./IMPORTANT_NOTES.md)** - Критически важные правила работы с сервером
   - НЕ предлагать проверку логов
   - Все ошибки в ответе API
   - Информация о сервере Beget
   - Обработка ошибок

2. **[.cursorrules](./.cursorrules)** - Правила для AI ассистента
   - Дублирует важные правила из IMPORTANT_NOTES.md
   - Обработка ошибок
   - Принципы работы

3. **[REQUEST_CONCEPT.md](./REQUEST_CONCEPT.md)** - Концепция заявок
   - Все статусы заявок и их логика
   - Жизненный цикл заявок по типам (wasteLocation, speedCleanup, event)
   - Cron задачи и автоматические действия
   - Push-уведомления

4. **[CHAT_CONCEPT.md](./CHAT_CONCEPT.md)** - Концепция чатов
   - Три типа чатов: support (техподдержка), private (пользователь-создатель), group (общий чат заявки)
   - Структура БД (chats, messages, message_reads, chat_participants)
   - WebSocket события (Socket.io)
   - API endpoints
   - Автоматическое создание/удаление чатов

### Документация API
5. **[API_DOCUMENTATION.md](./API_DOCUMENTATION.md)** - Полная документация API
   - Все endpoints
   - Модели данных
   - Формат дат и времени (UTC ISO 8601)
   - **Важно:** Все ID пользователей должны быть UUID из БД

### Структура проекта
6. **Серверная часть:**
   - `api/` - API роуты и логика
   - `scripts/cronTasks.js` - Cron задачи
   - `database/` - SQL миграции

7. **Клиентская часть:**
   - `joy_pick_app/` - Flutter приложение

## 🔧 Технические детали

### Сервер
- **Платформа:** Beget (ocelot)
- **Node.js:** v18.19.0
- **База данных:** MySQL
- **Cron:** node-cron (внутри приложения, не системный cron)
- **Домен:** autogie1.bget.ru

### Важные особенности
- **ID пользователей:** Везде используются UUID из БД (поле `id` из таблицы `users`), НЕ Firebase UID
- **Даты:** Все даты в UTC, формат ISO 8601
- **Ошибки:** Все ошибки возвращаются в JSON ответе API с детальной информацией
- **Логи:** У пользователя НЕТ доступа к логам сервера

### Cron задачи
- `autoCompleteSpeedCleanup` - автоматическое завершение speedCleanup через 24 часа после одобрения
- `checkWasteReminders` - напоминания исполнителю за 2 часа до окончания срока (22 часа после присоединения)
- `checkExpiredWasteJoins` - проверка истекших присоединений (24 часа)
- `notifyInactiveWasteRequests` - уведомление о скором удалении неактивных заявок (через 1 день, для тестирования)
- `deleteInactiveRequests` - удаление неактивных заявок (через 2 дня, для тестирования, потом вернуть на 8 дней)
- `checkEventTimes` - проверка времени до события (24 часа, 2 часа, начало)

### Планируемые функции
- **Чат система:** Реализация чатов согласно `CHAT_CONCEPT.md`
  - WebSocket сервер (Socket.io)
  - API endpoints для чатов
  - Автоматическое создание/удаление чатов
  - Push-уведомления для чатов

## 🎯 Ближайшие шаги

1. Реализовать систему чатов согласно `CHAT_CONCEPT.md`:
   - Создать SQL миграцию для таблиц чатов
   - Реализовать Socket.io сервер
   - Реализовать API endpoints для чатов
   - Реализовать автоматическое создание/удаление чатов
   - Добавить push-уведомления для чатов
2. Проверить, что на сервере обновлен `scripts/cronTasks.js`
3. Убедиться, что все ID пользователей в API используют UUID из БД

## ⚠️ Известные проблемы

1. **SQL синтаксис в cron задачах:** На сервере может быть старая версия с `INTERVAL 22 HOUR 1 MINUTE` вместо `INTERVAL 1320 MINUTE`
2. **ID пользователей:** Нужно убедиться, что фронтенд везде отправляет UUID из БД, а не Firebase UID

## 📝 Примечания

- Все изменения должны соответствовать правилам из `.cursorrules` и `IMPORTANT_NOTES.md`
- При работе с ID пользователей всегда использовать UUID из БД
- Все ошибки должны возвращаться в JSON ответе API с детальной информацией

